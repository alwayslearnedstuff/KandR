//Exercise 1-6. Verify that the expression getchar() != EOF is 0 or 1. 

//on ideone.com, you type the character in the input field then submit (i.e. run)

#include <stdio.h>

int main(void) {
	int c;
  
while ((c=getchar())!= EOF){
	printf("%d ",c!=EOF); 
	putchar(c);

}
printf("%d",c!=EOF);
	return 0;
}
